import {StoredMetadata} from "./MetadataManager";

export namespace MetadataSerializer {
    export function serialize(data: Record<string, StoredMetadata>): string {
        return
    }

    export function deserialize(string: string): Record<string, StoredMetadata> {
        return {};
    }
}